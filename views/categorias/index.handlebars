<h2>Lista de categorias</h2>
<hr>
{{#each categorias}}
    <a href="/categorias/{{slug}}"><h5>{{nome}}</h5></a>
    <div class="card mt-4">
        <div class="card-body">
            <h4>Nome: {{nome}}
            </h4>
            <small>Slug: {{slug}}</small>
            <br>
            <small>Data de criacao: {{#formatDate date}}{{/formatDate}}</small>
            <br>
        </div>
    </div>
{{else}}
<h4 class="mt-3">Lista vazia</h4>
{{/each}}